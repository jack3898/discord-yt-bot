/**
 * Some error happened, the queue is empty or something else. Disconnect now.
 */
export const DISCONNECT = 'DISCONNECT';

/**
 * Doesn't matter what happened, it's complete and the bot should play the next resource.
 */
export const COMPLETE = 'COMPLETE';
